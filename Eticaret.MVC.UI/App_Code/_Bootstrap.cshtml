@helper LinkButton(string id = "", string url = "", string color = "", string addtionalClass = "", string title = "", string icon = "", string text = "")
{
    <a href="@url" id="@id" name="@id" class="btn btn-@color @addtionalClass" title="@title">
        <span class="glyphicon glyphicon-@icon"></span> @text
    </a>
}
@helper LinkButton(string url = "", string color = "", string addtionalClass = "", string title = "", string icon = "", string text = "")
{
    <a href="@url" class="btn btn-@color @addtionalClass" title="@title">
        <span class="glyphicon glyphicon-@icon"></span> @text
    </a>
}

@helper Button(string color = "", string addtionalClass = "", string typp = "button", string icon = "", string text = "")
{
    <button type="@typp" class="btn btn-@color @addtionalClass">
        <span class="glyphicon glyphicon-@icon"></span> @text
    </button>
}
@helper Button(string id = "", string color = "", string addtionalClass = "", string typp = "button", string icon = "", string text = "")
{
    <button type="@typp" id="@id" name="@id" class="btn btn-@color @addtionalClass">
        <span class="glyphicon glyphicon-@icon"></span> @text
    </button>
}
@helper LinkMenu(string url = "", string icon = "", string text = "")
{
    <a class="dropdown-item" href="@url">
        <span class="glyphicon glyphicon-@icon"></span> @text
    </a>
}
@helper ModalPopup(string id ="myModal", string title="Modal Tittle",bool showCloseButton = true )
{ 
    <div class="modal fade" id="@id" name="@id" tabindex="-1" role="dialog" aria-labelledby="@(id + "label")" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header" id="@(id + "_header")">
                    <h5 class="modal-title" id="@(id + "_label")">@title</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body" id="@(id + "_body")">

                </div>
                <div class="modal-footer" id="@(id + "_footer")">
                    @if (showCloseButton)
                    {
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Kapat</button>
                    }
                </div>
            </div>
        </div>
    </div>
    }